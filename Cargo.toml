[package]
name = "bevy_tokio_runtime"
version = "0.2.0"
edition = "2021"

[dependencies]
bevy_app = { git = "https://github.com/bevyengine/bevy" }
bevy_ecs = { git = "https://github.com/bevyengine/bevy" }
tokio = { version = "1.32.0", features = ["rt-multi-thread", "sync"] }
